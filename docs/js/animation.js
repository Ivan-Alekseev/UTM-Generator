/*let animationCtrl = function () {
        let rotationDeg = 0;
        setInterval(rotation, 10);
        function rotation () {
        
        rotationDeg += 0.5;
        
        document.querySelector(".main").style.background = 
        `linear-gradient(${rotationDeg}deg, #7F7FD5, #86A8E7,#91EAE4)`;
    }
        
}
//События анимации фона - ajy 
    document.addEventListener("DOMContentLoaded", ready);
    function ready () {
       animationCtrl();
}*/

